# Database Configuration
# For production, use a PostgreSQL database URL
# For local development, you can use SQLite (dev.db)
DATABASE_URL="postgresql://user:password@localhost:5432/pharmasite?schema=public"

# NextAuth Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Resend Email Service (Optional)
# Sign up at https://resend.com to get an API key
# If not provided, email functionality will be disabled
RESEND_API_KEY="re_your_resend_api_key_here"
